
**Learning:** When adding words to JSON arrays like `wordbanks/wordbank-spring.json`, throwaway scripts (like Python) are effective but MUST be deleted before finalizing the PR to adhere to strict repository boundaries.
**Action:** Always delete `add_colors.py` or equivalent temporary scripts immediately after successfully running them and verifying their JSON output.
