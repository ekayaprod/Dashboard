# The Author Journal
**Learning:** Found a `README.md` for the Sidebar Productivity Suite that lacked a clear, actionable Quick Start guide. However, `npm` setup commands were present in the Quick Start. Looking closer at my instruction, it seems I need to clean up `README.md` and make it visually compliant with markdownlint while ensuring `npm ci` is used instead of `npm install`, as `npm ci` is the mandate in `.github/workflows/ci.yml`.

Wait, the instructions say:
* The System Memory says: Project dependencies should be installed using `npm ci`.
* The `README.md` says: `npm install`.

I need to rewrite the Quick Start guide in `README.md` to be a clear, visually appealing code block that uses `npm ci` instead of `npm install` and properly formats the instructions.
